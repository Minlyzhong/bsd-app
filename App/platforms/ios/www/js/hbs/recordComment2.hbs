{{#.}}
	<div class="comments1" data-commentId = "{{id}}">
		<span class="ownName one" data-id="{{userId}}">{{userName}}</span>
		{{#compare reviewerId '!=' 0}}
		 回复 <span class="ownName two" data-id="{{reviewerId}}">{{reviewerName}}</span>
		{{else}}
		说
		{{/compare}}
		: {{comment}}
	</div>
{{/.}}